module.exports = {
    plugins: {
      'tailwindcss/nesting': {}, // Ensure the nesting plugin is included if needed
      'tailwindcss': {},          // Ensure this is mentioned as per your current setup
      'autoprefixer': {},
    },
  }